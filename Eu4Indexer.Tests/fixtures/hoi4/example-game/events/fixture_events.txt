# Synthetic HOI4-format events. No Paradox content.
namespace = fixture

country_event = {
	id = fixture.1
	title = fixture.1.t
	desc = fixture.1.d
	is_triggered_only = yes

	option = {
		name = fixture.1.a
		set_country_flag = fixture_started
		country_event = { id = fixture.2 }
	}
}

country_event = {
	id = fixture.2
	title = fixture.2.t
	desc = fixture.2.d

	trigger = {
		has_country_flag = fixture_started
		has_completed_focus = fixture_focus_1
	}

	mean_time_to_happen = { days = 30 }

	option = {
		name = fixture.2.a
		add_stability = 1
		set_country_flag = fixture_done
	}
}
